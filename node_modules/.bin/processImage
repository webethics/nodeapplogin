#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../express-processimage/bin/processImage" "$@"
  ret=$?
else 
  node  "$basedir/../express-processimage/bin/processImage" "$@"
  ret=$?
fi
exit $ret
